services:

  tcp-echo:
    image: alpine/socat:latest
    command: ["-v", "tcp-listen:9000,reuseaddr,fork", "exec:/bin/cat"]

  server:
    image: ghcr.io/hrnco/http2tcp-signing-server:1.15
    ports:
      - "8080:80"
    environment:
      APP_ENV: dev
      TEST_DEFAULT_DEVICE_PORT: 9000
      TEST_DEFAULT_DEVICE_IP: tcp-echo
    volumes:
      - keys:/keys

  agent:
    image: ghcr.io/hrnco/http2tcp-local-agent:latest
#    build:
#      context: .
#      dockerfile: .docker/Dockerfile
    ports:
      - "34279:80"
    environment:
      HTTP2TCP_CORS_ALLOW_ORIGIN: http://localhost:8080
    volumes:
      - ./:/var/www/html
      - agent_data:/data

volumes:
  keys:
  agent_data:
