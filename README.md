# http2tcp-local-agent
a lightweight local HTTP server running in Docker that allows web or cloud applications to securely communicate with devices on the LAN over TCP — without needing to open ports, set up a VPN, or expose the LAN to the internet
